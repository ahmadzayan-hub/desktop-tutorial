// =====================================================================
// config.js — كل الإعدادات في مكان واحد.
// مفيش أي سر هنا (التوكن والمفتاح في .env بس). دي إعدادات سلوك بس.
// =====================================================================

module.exports = {
  // ---- مين الـ agent بيكلّمه؟ أنا أنا بس ----
  // chat_id بتاعي على تيليجرام. سيبه فاضي في أول مرة:
  // شغّل البوت، ابعتله /start، وهو هيطبع لك الـ id في الكونسول.
  // بعدها حطه هنا (رقم زي 123456789) وأعد التشغيل.
  chatId: process.env.MY_CHAT_ID || '',

  // ---- الجدولة (cron) بتوقيت الإمارات ----
  // الصيغة: دقيقة ساعة يوم-الشهر شهر يوم-الأسبوع
  morningCron: '0 7 * * *', // كل يوم 7:00 صباحاً
  eveningCron: '0 21 * * *', // كل يوم 9:00 مساءً
  weeklyReviewCron: '0 20 * * 5', // كل جمعة 8:00 مساءً (تقرير أسبوعي)
  timezone: 'Asia/Dubai',

  // ---- اللغة والنبرة ----
  language: 'ar-EG', // عربي مصري
  tone: 'دافئة، صادقة، بسيطة، من غير مبالغة ولا كلام مصنوع',

  // ---- مواضيع الاقتراحات (للتنويع والترجيح) ----
  themes: [
    'امتنان',
    'اشتياق',
    'تمني يوم جميل',
    'تقدير',
    'دعم',
    'دعاء',
    'كلمة من القلب',
  ],

  // كام يوم نتجنّب فيهم تكرار نفس الموضوع (تنويع).
  avoidRecentThemeDays: 3,

  // ---- المناسبات ----
  // عيد الجواز وعيد ميلاد الزوجة: تواريخ ثابتة بالميلادي بصيغة MM-DD.
  // الأعياد الإسلامية: 'manual' — انت تدخّل التاريخ المؤكد كل سنة YYYY-MM-DD.
  // بما إن الاقتراح بيوصل لك انت بس، فأي خطأ في التاريخ ما بيوصلش لزوجتك.
  // ما نعتمدش على حساب أعمى للأعياد الإسلامية.
  occasions: {
    // املا التواريخ دي:
    weddingAnniversary: { type: 'fixed', date: 'MM-DD', label: 'عيد جوازنا' },
    wifeBirthday: { type: 'fixed', date: 'MM-DD', label: 'عيد ميلاد مراتي' },

    // الأعياد الإسلامية — حدّث الـ YYYY-MM-DD كل سنة من تقويم موثوق:
    hijriNewYear: { type: 'manual', date: 'YYYY-MM-DD', label: 'رأس السنة الهجرية' },
    mawlid: { type: 'manual', date: 'YYYY-MM-DD', label: 'المولد النبوي' },
    ramadanStart: { type: 'manual', date: 'YYYY-MM-DD', label: 'أول رمضان' },
    eidFitr: { type: 'manual', date: 'YYYY-MM-DD', label: 'عيد الفطر' },
    eidAdha: { type: 'manual', date: 'YYYY-MM-DD', label: 'عيد الأضحى' },
  },

  // ---- عقل الـ AI ----
  groqModel: 'llama-3.3-70b-versatile', // موديل مجاني قوي على Groq

  // ---- التعلّم بالسياق ----
  // أقصى عدد أمثلة أسلوب نحتفظ بيها (آخر اختياراتك). الأقدم يخرج.
  styleExamplesMax: 30,

  // ---- وضع الأمان ----
  // dryRun=true أثناء التطوير: يطبع الاقتراح في الكونسول بدل ما يبعته.
  // خلّيه true لحد ما تجرّب كله، وبعدين حطه false عشان البوت يبعت لك فعلاً.
  dryRun: true,
};
