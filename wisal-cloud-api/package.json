{
  "name": "wisal-cloud-api",
  "version": "0.1.0",
  "private": true,
  "description": "WhatsApp Business Cloud API backend for Wisal (compliant customer messaging).",
  "type": "commonjs",
  "scripts": {
    "check": "node --check api/send.js && node --check api/webhook.js && node --check lib/wa.js && node --check test/wa.test.js",
    "test": "node test/wa.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
