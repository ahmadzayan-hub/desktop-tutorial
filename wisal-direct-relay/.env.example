# wisal-direct-relay — لا أسرار تشغيل مطلوبة في هذه الشريحة (التحقق كله
# بتوقيعات المفتاح العام المُرسَلة مع كل طلب، مفيش مفتاح API مشترك).
#
# قبل أي إطلاق إنتاج: هذا المتغيّر غير موجود بعد لأن التخزين في الذاكرة
# (lib/store.js) — لازم تستبدله بـ storage حقيقي (مثلاً DATABASE_URL لـ
# Postgres) قبل النشر، موثّق كـ blocker في README.md وADR-002.
DATABASE_URL=
