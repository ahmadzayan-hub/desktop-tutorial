{
  "name": "wisal-direct-relay",
  "version": "0.1.0",
  "private": true,
  "description": "Store-and-forward relay for Wisal Direct (E2EE) — device registry + opaque encrypted envelopes. Not the E2EE protocol itself (see docs/decisions/ADR-002-e2ee-protocol.md).",
  "type": "commonjs",
  "scripts": {
    "check": "node --check api/relay.js && node --check lib/crypto.js && node --check lib/store.js && node --check lib/relay.js && node --check lib/log.js && node --check lib/limiter.js && node --check lib/sharedStore.js && node --check test/relay.test.js && node --check test/plaintext-leak.test.js && node --check test/http-dispatch.test.js",
    "test": "node test/relay.test.js && node test/plaintext-leak.test.js && node test/http-dispatch.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
