# Beyond Coffee Moments — front-end env (all optional for the demo)

# Route AI generation + image moderation server-side (recommended for production).
# When unset, the app uses built-in deterministic templates/heuristics.
VITE_AI_ENDPOINT=

# Payment / WhatsApp / analytics keys belong on the server, not here.
# See README for the recommended UAE stack (Telr/PayTabs + Tabby/Tamara + Apple/Google Pay).
