# Cost governance (spec section 25). TODO values need the owner's numbers;
# until set, ceilings are treated as "not configured" (no fabricated limits).
cost-currency: USD
per-session-cost-ceiling: TODO
weekly-cost-ceiling: TODO
heavy-call-limit-per-session: 3
default-limits:
  max-agent-turns: 12
  max-tool-calls: 20
  max-retries: 1
  max-heavy-calls: 3
  max-runtime-seconds: 1800
