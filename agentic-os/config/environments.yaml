# Environment policy (spec section 21). Secure defaults.
default-environment: sandbox
production-actions-enabled: false
external-actions-enabled: false
environments:
  sandbox:
    production-secrets: forbidden
  development:
    changes-must-be: reversible
  staging:
    requires: verification
  production:
    requires: explicit-approval
test-data-restricted-sources: approval-required
