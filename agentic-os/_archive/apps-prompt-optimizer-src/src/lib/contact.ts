/** Single source of truth for contact info shown across the site. */
export const CONTACT_EMAIL = "support@tweenz.ae";
export const CONTACT_MAILTO = `mailto:${CONTACT_EMAIL}?subject=${encodeURIComponent(
  "Tweenz AI — ملاحظات / دعم"
)}`;
