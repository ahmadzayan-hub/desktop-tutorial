{
  "framework": "nextjs",
  "buildCommand": "npm run build",
  "devCommand": "npm run dev",
  "installCommand": "npm install",
  "outputDirectory": ".next",
  "regions": ["iad1"],
  "ignoreCommand": "git diff HEAD^ HEAD --quiet -- . ':(exclude)vertex-platform'",
  "env": {
    "NEXT_PUBLIC_DEMO_MODE": "true"
  }
}
