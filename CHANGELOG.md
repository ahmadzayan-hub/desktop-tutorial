# سجل التغييرات — وصال (Wisal)

كل التغييرات المهمة للمنتج (أندرويد + سطح المكتب + الويب + الباك-إند).
الأحدث فوق. البناءات بتتنشر تلقائيًا على `android-latest` و`windows-latest`.

## 2026-08-09

### أندرويد
- 🔥 **سلسلة الدفء**: عدّاد أيام التواصل المتتالية (بسماحية يوم) من سجل الرسايل
  المبعوتة فعلاً — كارت متدرّج الألوان على الرئيسية برسالة تشجيع متدرّجة.
- 🤖 **شفافية الذكاء**: إشعار أول تشغيل بيوضّح إن الاقتراحات من ذكاء اصطناعي،
  وإن السياق بيتبعت لـ Groq وقت التوليد، وإن البيانات محلية والمفتاح مشفّر،
  ومفيش إرسال تلقائي — بإقرار المستخدم.

### سطح المكتب
- 🔥 سلسلة الدفء في شريط الحالة (نفس منطق أندرويد).

### الويب (wisal-web)
- كروت مزايا جديدة: عربي/إنجليزي، وضع الأعمال، سلسلة الدفء + تحديث JSON-LD.

## 2026-08-04

### أندرويد — اختبارات
- اختبارات Compose لكل الشاشات الرئيسية (Welcome/History/Polish/SmartReply/Broadcast)
  بتشتغل في CI بدون emulator (Robolectric).

## 2026-08-03

### أندرويد
- ✨ **حسّن رسالتي**: تكتب مسوّدتك ويرجّعها نسختين أدفأ بأسلوب الشخص ولغته.
- 🌉 **إعادة تواصل**: نيّة جديدة بتكسر برود الانقطاع — مربوطة بلفتة «بقالك فترة».
- 😄 **نبرة سريعة**: مرح/جاد/اعتذار/امتنان/طمأنة بضغطة على أي اقتراح.
- 🗓️ **رادار المناسبات+**: تنبيه مبكّر للمناسبات العامة (مش بس مناسبات الأشخاص)
  + زر كتم لكل مناسبة.
- 🌐 **لغة حسب المستقبل**: تلقائي/عربي/إنجليزي لكل شخص — كل التوليد بيحترمها.
- 💼 **وضع الأعمال**: رد مهني دافئ على عملاء واتساب Business، بضغطة لكل عميل
  (مفيش بلاست تلقائي)، مع حسابات مُرسِل متعددة (كود دولة + توقيع + التطبيق الهدف).
- 📸 **جسر المنصّات**: تجهيز الرسالة ونسخها وفتح إنستجرام/ماسنجر/لينكدإن.
- 🚀 **WhatsApp Business Cloud API**: باك-إند سيرفرلس (`wisal-cloud-api/`) + ربط
  اختياري في التطبيق (نص + قوالب معتمدة) — الإرسال الآلي المشروع الوحيد عبر Meta.
- ⚡ **أداء**: توليد رسائل العملاء متوازي (٤ معًا)، R8/minify + تقليص الموارد
  (APK ~1.6MB)، LazyColumn للسجل والبثّ، تأجيل WorkManager عن أول فريم.
- 🧪 **طبقة اختبار**: Robolectric لطبقة البيانات (بتحرس دوران JSON تحت R8)
  + اختبارات منطق نقي (هاتف/CSV/تواريخ/لغة/سلسلة/روابط منصّات/عقد الباك-إند).

### سطح المكتب
- تكافؤ: لغة المستقبل + إعادة تواصل + نبرة الأعمال.

### توثيق
- `docs/EXPERT_ASSESSMENT.md` (تقييم خبير + فجوات) · `docs/INTEGRATIONS.md`
  (الممكن والمستحيل في ربط المنصّات + خارطة Cloud API) · `wisal-cloud-api/README.md`.

## أقدم
- التوليد الأساسي بنيّات ومواضيع مرجّحة + تعلّم أسلوب لكل شخص (few-shot محلي).
- رد ذكي 💬 على الرسايل الواردة · أفكار هدايا 🎁 · لفتة جاية 💡 · مفضّلة وسجل.
- بثّ مجموعات آمن (ضغطة لكل شخص) من جهات الاتصال أو CSV.
- Material 3 Expressive بدون تأثيرات مزعجة · إشعارات صباح/مساء · نسخ احتياطي.
